entities:
- name: http_client
  namespace: laplacian.arch.api.service

  properties:
  - name: name
    type: string
    primary_key: true

  - name: base_uri
    type: string
    optional: true

  - name: api_key
    type: string
    optional: true
